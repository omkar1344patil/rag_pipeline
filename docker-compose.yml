services:
  rag-backend:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    restart: unless-stopped
    # HuggingFace model cache persists across rebuilds
    volumes:
      - hf_cache:/root/.cache/huggingface

volumes:
  hf_cache: